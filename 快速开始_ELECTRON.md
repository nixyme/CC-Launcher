# 快速开始 - Electron 版

## 🚀 5分钟快速上手

### 第一步: 安装依赖

打开终端,进入项目目录:

```bash
cd "/Users/nixy/Library/Mobile Documents/com~apple~CloudDocs/AICODE/Claude Code/test_002/CC万能启动器"
```

安装 Node.js 依赖:

```bash
npm install
```

安装 Python 依赖:

```bash
pip3 install -r backend/requirements.txt
```

### 第二步: 启动应用

**推荐方式 - 使用启动脚本:**

```bash
./start.sh
```

**或者手动启动:**

终端1 - 启动后端:
```bash
python3 backend/server.py
```

终端2 - 启动前端:
```bash
npm start
```

### 第三步: 使用应用

1. **添加项目**
   - 点击左上角的 ➕ 按钮
   - 填写项目信息:
     - 项目名称: 例如 "我的AI项目"
     - 项目路径: 项目文件夹的完整路径
     - 默认命令: `claude --dangerously-skip-permissions /plan 帮我开发一个功能`
     - 结果路径: 保存结果的文件夹路径
   - 点击"保存"

2. **执行命令**
   - 从左侧列表选择项目
   - 在右侧查看/编辑命令
   - 点击"▶️ 执行命令"按钮
   - 命令会在新的终端窗口中运行

3. **查看结果**
   - 点击"📂 打开文件夹"按钮
   - 会在 Finder 中打开结果文件夹

4. **编辑项目**
   - 选择项目后点击"✏️ 编辑"
   - 修改项目信息
   - 点击"保存"

5. **删除项目**
   - 选择项目后点击"🗑️ 删除"
   - 确认删除操作

## 💡 使用技巧

### 命令模板

常用的 Claude Code 命令:

```bash
# 规划模式
claude --dangerously-skip-permissions /plan 帮我规划这个功能的实现

# 工作流模式
claude --dangerously-skip-permissions /zcf:workflow 实现用户登录功能

# 功能开发
claude --dangerously-skip-permissions /zcf:feat 添加数据导出功能

# Git 提交
claude --dangerously-skip-permissions /zcf:git-commit
```

### 快速编辑命令

- 点击项目后,直接在右侧的命令框中修改
- 修改后点击"💾 更新命令"保存
- 下次执行时会使用新命令

### 项目路径示例

```bash
# macOS/Linux
/Users/username/projects/my-project

# Windows
C:\Users\username\projects\my-project
```

## ⚙️ 系统要求

- **Node.js**: 16.x 或更高版本
- **Python**: 3.8 或更高版本
- **操作系统**: macOS, Linux, 或 Windows

## 🔧 故障排除

### 问题: 后端无法启动

**解决方案:**
```bash
# 检查 Python 版本
python3 --version

# 重新安装依赖
pip3 install -r backend/requirements.txt
```

### 问题: Electron 窗口无法打开

**解决方案:**
```bash
# 删除 node_modules 重新安装
rm -rf node_modules
npm install
```

### 问题: 端口被占用

**解决方案:**
```bash
# 查找占用 5000 端口的进程
lsof -i :5000

# 结束进程
kill -9 <PID>
```

或者修改端口:
- 编辑 `backend/server.py`: 修改端口号
- 编辑 `electron/preload.js`: 修改 `apiUrl`

### 问题: 命令执行没反应

**检查项:**
1. 确认项目路径存在
2. 确认命令格式正确
3. 检查终端是否有权限
4. 查看后端日志是否有错误

## 📚 下一步

- 阅读 [README_ELECTRON.md](README_ELECTRON.md) 了解更多细节
- 查看 [需求.md](需求.md) 了解功能需求
- 探索 API 接口进行二次开发

## 🎉 开始使用

现在你已经准备好使用 Claude Code 万能启动器了!

如果遇到问题,请检查:
1. 依赖是否正确安装
2. 端口是否被占用
3. 路径是否正确
4. 权限是否足够

祝使用愉快! 🚀
